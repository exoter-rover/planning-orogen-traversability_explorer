# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(traversability_explorerTaskLib)
ADD_LIBRARY(${TRAVERSABILITY_EXPLORER_TASKLIB_NAME} SHARED 
    ${TRAVERSABILITY_EXPLORER_TASKLIB_SOURCES})
add_dependencies(${TRAVERSABILITY_EXPLORER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TRAVERSABILITY_EXPLORER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TRAVERSABILITY_EXPLORER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TRAVERSABILITY_EXPLORER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TRAVERSABILITY_EXPLORER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TRAVERSABILITY_EXPLORER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TRAVERSABILITY_EXPLORER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TRAVERSABILITY_EXPLORER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TRAVERSABILITY_EXPLORER_TASKLIB_HEADERS}
    DESTINATION include/orocos/traversability_explorer)

